package models.ui_elements;

import processing.core.PApplet;

public class UIMultiLabel extends UIComponent {
  protected UIComponent SubComponent;

  public UIMultiLabel(PApplet applet, float x, float y, float z) {
    super(applet, x, y, z);
  }
}
