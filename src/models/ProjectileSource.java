package models;

// This code is defining an enumeration called `ProjectileSource` with two possible values: `Player`
// and `Enemy`. This enumeration can be used to represent the source of a projectile in a game or
// simulation.
public enum ProjectileSource {
  Player,
  Enemy
}
